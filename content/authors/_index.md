---
# To publish author profile pages, remove all the `build` (updated to build from _build in 2025) and `cascade` settings below.
build:
  render: never
cascade:
  build:
    render: never
    list: always
---
